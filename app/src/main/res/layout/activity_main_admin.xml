<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main_admin"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="ifpb.edu.br.spreejpa.activity.MainAdminActivity"
    android:orientation="vertical">

    <Button
        android:id="@+id/btnCadastroCategoria"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:text="Cadastro Categoria"
        android:textSize="20dp"
        android:background="@color/gray"
        android:textColor="@color/orange"
        />

    <Button
        android:id="@+id/btnCadastroEvento"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:text="Cadastro Evento"
        android:background="@color/orange"
        android:textColor="@color/gray"
        android:textSize="20dp"/>

</LinearLayout>
